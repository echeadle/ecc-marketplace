---
name: example-command
description: A template slash command
---

This is a command that runs when the user types `/plugin-template:example-command`.

Commands are similar to skills but are typically shorter, action-oriented
instructions rather than complex workflows.

Do the thing the user asked for.
